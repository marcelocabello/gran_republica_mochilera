name: gran_republica_mochilera
description: "App Gran República Mochilera"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^12.0.1
  provider: ^6.0.5
  carousel_slider: ^5.1.1
  google_fonts: ^6.2.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
